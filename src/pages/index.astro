---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
import ImageViewer from '../components/ImageViewer.astro';
import TestimonialCard from '../components/TestimonialCard.astro';
import { Image } from 'astro:assets';
import { testimonials } from '../data/testimonials';

const heroImage = {
  url: "https://github.com/ashwin-nat/pits-n-giggles/blob/main/screenshots/main-ui.png?raw=true",
  alt: "Pits n' Giggles Telemetry Dashboard",
  description: "The main dashboard showing real-time telemetry data during a race"
};
---

<Layout title="Home">
  <!-- Hero Section -->
  <section class="pt-32 pb-20 relative">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-12">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6">Advanced Self-Engineering tool for F1 Sim Racing</h1>
        <p class="text-xl text-secondary-300 mb-8">Take your sim racing to the next level with real-time data and performance analytics at a glance without any user input</p>
        <div class="flex flex-wrap justify-center gap-4">
          <Button href="/features" variant="outline" size="lg">Explore Features</Button>
          <Button href="/releases" variant="primary" size="lg">Download Now</Button>
        </div>
      </div>
    </div>

    <!-- Hero Image -->
    <div class="relative mx-auto max-w-5xl">
      <div class="bg-gradient-to-tr from-primary-500/20 to-accent-500/20 rounded-xl p-2 shadow-xl cursor-pointer" data-screenshot>
        <Image
          src={heroImage.url}
          alt={heroImage.alt}
          width={1920}
          height={1080}
          class="rounded-lg w-full h-auto shadow-md"
        />
      </div>
    </div>
  </section>

  <!-- App Description Section -->
  <section class="py-20 bg-secondary-900/50">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold mb-8 text-center">What is Pits n' Giggles?</h2>
        <div class="prose prose-invert prose-lg mx-auto">
          <p class="text-secondary-300 mb-6">
            Pits n' Giggles is a powerful telemetry tool designed specifically for F1 sim racing enthusiasts. It provides real-time data analysis and performance insights during your races, helping you make informed decisions and improve your racing strategy.
          </p>
          <p class="text-secondary-300 mb-6">
            Whether you're a casual racer or a competitive driver, our tool offers valuable insights into your driving performance, including tire wear, fuel management, ERS deployment, and detailed lap time analysis. The intuitive interface ensures you can focus on racing while having all critical information at your fingertips.
          </p>
          <p class="text-secondary-300">
            With features like real-time telemetry, advanced analytics, and OBS integration for streamers, Pits n' Giggles is your perfect companion for enhancing your F1 sim racing experience.
          </p>
        </div>
      </div>
    </div>
  </section>

  {testimonials.length > 0 && (
    <section class="py-20">
      <div class="container mx-auto px-4">
        <div class="text-center max-w-3xl mx-auto mb-16">
          <h2 class="text-3xl md:text-4xl font-bold mb-4">What Racers Say</h2>
          <p class="text-secondary-300">Hear from sim racers who improved their performance with Pits n' Giggles.</p>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {testimonials.map(testimonial => (
            <TestimonialCard {...testimonial} />
          ))}
        </div>
      </div>
    </section>
  )}

  <ImageViewer images={[heroImage]} />
</Layout>